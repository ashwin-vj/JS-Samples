var numbers = [1, 2, 3, 4, 5];
var slicedNumbers = numbers.slice(1, 4);
console.log(slicedNumbers);       // Output: [2, 3, 4]

// it slices the the part we need to cut.
//  syntax:     array.slice(start, end);
